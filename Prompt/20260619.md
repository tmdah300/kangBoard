# Handover Prompt for the Next Session

Please copy and paste the following directly into Claude Code.

---

## Handover Prompt

I am building an anonymous message board using ASP.NET Core Web API, Vue 3, and TypeScript.
In the previous session, I finished setting up the backend and frontend infrastructure,
so please continue from there in this session.

### Project Structure

```
C:\Users\kang_seungmo\Documents\Practice\Board_geachu\
├── BoardApi/               # ASP.NET Core Web API (.NET 5.0)
│   ├── Models/
│   │   ├── Post.cs         # ID, Title, Content, CreatedAt, ViewCount, LikeCount
│   │   └── Comment.cs      # ID, PostId, Content, CreatedAt
│   ├── Data/
│   │   └── AppDbContext.cs  # DbSet<Post>, DbSet<Comment>
│   ├── Migrations/         # InitialCreate（SQL Server用）
│   ├── Startup.cs          # UseSqlServer登録済み
│   ├── appsettings.json    # Server=.\SQLEXPRESS;Database=BoardDb
│   └── BoardApi.csproj     # EFCore.SqlServer 5.0.17, Tools, Design
├── board-front/            # Vue3 + TypeScript + Vite
│   └── src/
│       ├── api/
│       │   ├── client.ts   # Axios baseURL: http://localhost:5000/api
│       │   ├── posts.ts    # getPosts / getPost / createPost
│       │   └── comments.ts # getComments / createComment
│       ├── types/
│       │   ├── post.ts     # Post, CreatePostRequest インターフェース
│       │   └── comment.ts  # Comment, CreateCommentRequest インターフェース
│       ├── views/
│       │   ├── HomeView.vue       # 投稿一覧
│       │   ├── PostDetailView.vue # 投稿詳細 + コメント
│       │   └── PostNewView.vue    # 新規投稿フォーム
│       └── router/index.ts # / , /posts/new , /posts/:id
└── BoardApi.sln            # Visual Studioソリューション

```

### 完了済みの作業

- [x] Create an ASP.NET Core Web API project
- [x] Define the Post and Comment models
- [x] Create AppDbContext
- [x] Configure the SQL Server (SQL Express) connection
- [x] Create an EF Core migration (InitialCreate)
- [x] Create a Visual Studio solution file
- [x] Create a Vue 3 + TypeScript + Vite project
- [x] Configure Vue Router (for 3 screens)
- [x] Configure Axios (API client)
- [x] Create type definition files (Post / Comment)
- [x] Create all view components (List / Detail / New)

### Next Steps (Incomplete)

- [ ] **Top Priority** Run `Update-Database` in Visual Studio PMC to create the database
- [ ] Create the ASP.NET Core PostsController
  - GET    /api/posts          Post List
  - GET    /api/posts/{id}     Post Details (View Count +1)
  - POST   /api/posts          Create Post
  - POST   /api/posts/{id}/comments  Create a comment
  - GET    /api/posts/{id}/comments  List comments
  - POST   /api/posts/{id}/like      Increase likes by 1
- [ ] Configure CORS (Vue: localhost:5173 → API: localhost:5000)
- [ ] Test functionality by actually calling the API from the frontend

### Environment Information

- OS: Windows 11
- .NET SDK: 5.0.416
- Node.js: v24.13.0
- DB: SQL Server Express (SQLEXPRESS)
- Both VS Code and Visual Studio are installed
- Use Git Bash instead of PowerShell to run npm, as PowerShell throws a script execution policy error

### Notes

- Launching the frontend: In the VS Code terminal (Git Bash), run `npm run dev` → http://localhost:5173
- Launching the API: Press F5 in Visual Studio or run `dotnet run` → http://localhost:5000
- PMC command: Visual Studio → Tools → NuGet Package Manager → Package Manager Console
